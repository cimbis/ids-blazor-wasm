IDS_UTILS_Layout_Columns_Width($columns = 12)
    $currentGutter = $columns - 1
    width "calc(var(--if-layout-helper-column-width) * %s + (%s * var(--if-layout-helper-gutter-width)))" % ($columns $currentGutter)

IDS_UTILS_Layout_Wide_Columns_Width($columns = 12)
    $currentGutter = $columns - 1
    width "calc(var(--if-layout-helper-wide-column-width) * %s + (%s * var(--if-layout-helper-wide-gutter-width)))" % ($columns $currentGutter)

IDS_UTILS_Layout_Fluid_Columns_Width($columns = 12)
    $currentGutter = $columns - 1
    width "calc(var(--if-layout-helper-fluid-column-width) * %s + (%s * var(--if-layout-helper-fluid-gutter-width)))" % ($columns $currentGutter)

IDS_UTILS_Layout_Across_Columns_Width($columns = 12)
    $currentGutter = $columns - 1
    width "calc(var(--if-layout-helper-across-column-width) * %s + (%s * var(--if-layout-helper-across-gutter-width)))" % ($columns $currentGutter)
