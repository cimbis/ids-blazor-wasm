
IDS_UTIL_GenerateTextClasses($break)
  .if.u-text-left--{$break}
    text-align left
  .if.u-text-right--{$break}
    text-align right
  .if.u-text-center--{$break}
    text-align center

IDS_UTIL_GenerateText()
  .if.u-text-left
    text-align left
  .if.u-text-right
    text-align right
  .if.u-text-center
    text-align center
  for $break, $value in $breakpoint
    +IDS_BREAKPOINT_MQUp($break)
      IDS_UTIL_GenerateTextClasses($break)
